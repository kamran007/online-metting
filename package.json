{
  "name": "video-meeting-app",
  "version": "2.0.0",
  "private": true,
  "description": "Real-time video meetings (Next.js + LiveKit SFU + Firebase auth). Token minting runs in a Next API route — deployable to Vercel as a single app.",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:lan": "npm run dev:lan --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
