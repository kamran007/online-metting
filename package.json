{
  "name": "video-meeting-app",
  "version": "3.0.0",
  "private": true,
  "description": "Real-time video meetings: Next.js client + custom mediasoup SFU over wss + Firebase auth.",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n sfu,web -c magenta,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:lan": "npm run dev:lan --prefix client",
    "build": "npm run build --prefix client",
    "start:server": "npm run start --prefix server",
    "start:client": "npm run start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
